import { PanelEditorProps } from '@grafana/ui';

import React, { PureComponent } from 'react';
import { Options } from '../types';


export class AntdPanelEditor extends PureComponent<
  PanelEditorProps<Options>
> {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <>
        TODO!!!
      </>
    );
  }
}
